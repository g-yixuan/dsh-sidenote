/**
 * Workitem 02 — 划选注释：implemented on feat/quote-annotations.
 * Selection listener + numbered badges + annotation editor + composer chip.
 * Filled in by the WI-02 implementation.
 */
import type { Context } from '../../context-types.ts'

export function registerAnnotations(_ctx: Context): void {
  // TODO(WI-02)
}
