/**
 * Workitem 01 — 侧边聊天面板：implemented on feat/side-chat.
 * Registers the side-chat tab type with better-sidebar and wires fork /
 * restore / chat. Filled in by the WI-01 implementation.
 */
import type { Context } from '../../context-types.ts'

export function registerSideChat(_ctx: Context): void {
  // TODO(WI-01)
}
